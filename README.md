# Cataloger

Cataloger is a personal collection management project.

At the moment, the focus is on developing the REST API, which is built with [FastEndpoints](https://fast-endpoints.com/). The long-term goal is to build a desktop application that uses the API, with a mobile application potentially following later.
